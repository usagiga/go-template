name: "test"

on:
  push:
    branches:
      - main
    paths:
      - "**.go"
  pull_request:
    branches:
      - main
    paths:
      - "**.go"

jobs:
  test:
    name: "test"
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v3"
      - uses: "actions/setup-go@v3"
        with:
          go-version: "1.19.5"
      - name: "vet"
        run: "make lint"
      - name: "build"
        run: "make build"
      - name: "test"
        run: "make test"
